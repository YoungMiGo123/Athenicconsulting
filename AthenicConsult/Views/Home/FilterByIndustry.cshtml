@model FilterByIndustryViewModel

@{
    ViewData["Title"] = "Industry Filter Page";
}
<div class="row" style="margin-bottom:20px;">
    <div class="col-12">
        <div style="text-align:center;">
            <h1>
                Popular Brands in Industry: 
                <a class="btn btn-primary" asp-action="Index" asp-controller="Home">
                    @Model.Industry.Name
                    <span><i class="fa fa-close" style="font-size:20px"> </i></span>
                </a>
            </h1>
        </div>
    </div>
</div>


@{
   await Html.RenderPartialAsync("/Views/Shared/Components/_ExploreIndustryBrands.cshtml", Model.BrandViewModel);
}
